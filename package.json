{
  "name": "ottplay-swop",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "cf-typegen": "wrangler types",
    "typecheck": "tsc --noEmit",
    "build": "wrangler deploy --dry-run --outdir=dist",
    "build:terraform": "bash scripts/build-worker-for-terraform.sh",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.22.0",
    "@cloudflare/workers-types": "^5.20260903.1",
    "typescript": "^5.9.2",
    "vitest": "^4.1.11",
    "wrangler": "^4.34.0"
  }
}
